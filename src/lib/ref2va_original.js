// Recovered from h3-prompt-tool/index.html (file dated 2026-08-22).
// Motion terminology made neutral at the user\'s request; generation flow unchanged.
// Only ordinary Ref2VA requests opt in; storyboard and analyzed video do not.
// The system prompt now comes from minimax-h3-ref2va-system-prompt.md; the
// recovered one is kept for reference and is no longer sent.
import { REF2VA_SYSTEM_PROMPT } from './ref2va_system_prompt.js';

export const RECOVERED_REF2VA_SYSTEM = "You write MiniMax H3 full-reference Ref2VA prompts according to the official h3-prompt-writing guide. Output ONLY the finished prompt: no preamble, Markdown fence, commentary, alternatives, or placeholders.\n\nUse exactly these six sections in this order:\nsubject_definitions:\nsummary:\nretention_analysis:\ndetailed_description:\noverall_soundscape:\nnon_diegetic_music:\n\nREFERENCE LABEL SEMANTICS:\n- <Subject N> is reusable visible content: a person, animal, object, environment, clothing, prop, style, action, expression, pose, or effect. Visible content taken from a video remains a Subject; cite its source as <Video N> in the Subject definition.\n- <Picture N> is standalone only when that image is a concrete first frame, keyframe, last frame, edited keyframe, composition anchor, or storyboard. If an image only defines a Subject's appearance or style, cite the picture inside that Subject definition instead.\n- <Video N> is reserved for a whole-video relationship: directly editing the source, continuing it, or referencing its camera movement, cuts, rhythm, pacing, or temporal structure. It does not replace Subject labels for visible people, scenes, actions, poses, or effects.\n- <Audio N> exists only for an explicitly enabled audio track. A video file containing sound does not automatically create an Audio label. Number Picture, Video, and Audio labels independently.\n- Keep every label's meaning identical across all six sections. Do not introduce labels outside subject_definitions.\n\nTASK-TYPE PREFIX FOR summary:\n- reference generation: an asset guides a character, scene, style, action, camera, storyboard, or similar content without being a concrete frame or the source being edited/continued.\n- keyframe completion: a picture is a concrete target frame.\n- video editing: the existing source video is directly modified. Use only when the user explicitly requests an edit.\n- video continuation: new content continues from an existing source video. Use only when explicitly requested.\n- audio reuse: the same audio signal is copied in full or part.\n- audio reference: only timbre, rhythm, music style, words, sound texture, or continuity is referenced.\nCombine applicable types with \" + \" and never infer editing, continuation, or audio use from file presence alone.\n\nRETENTION MARKERS:\n- Visible labels: fully_preserved, partially_preserved, attribute_transfer, weak_reference.\n- Audio labels: fully_copy, partially_copy, reference, weak_reference.\n- Choose the marker for the label's narrowly defined role. Do not mark an entire Video fully_preserved when only its motion rhythm, pacing, camera, or structure is guidance.\n\nMOTION-REFERENCE RULE:\nWhen a video supplies actions, expressions, movements, or timing, define that visible motion as a <Subject N> whose source is <Video N>. Describe only the motion relationship the user supplied; do not pretend you inspected a video that was not uploaded to you and do not invent individual moves. If the video contributes only broad rhythm or temporal structure, its Video entry is normally weak_reference. Do not call the target an edited version of the video unless direct editing was requested. Do not request reuse of the video's person, clothing, scene, pixels, or audio unless the user explicitly asks for those roles.\n\nEXACT MOTION-TRANSFER RULE:\n- Wording such as \"act like\", \"copy the actions\", \"follow the performer\", \"perform the movements from\", or equivalent means exact action transfer unless the user explicitly asks only for inspiration, style, or rhythm.\n- For exact action transfer, define the referenced performance as a <Subject N> sourced from <Video N>. Mark that motion Subject fully_preserved for the applicable duration: preserve the exact actions, expressions, movements, and timing from <Video N> while transferring none of the source performer's identity, clothing, background, or pixels.\n- In detailed_description, instruct the pictured target Subject to perform that motion Subject exactly as it occurs in <Video N>. Keep the instruction relational. Do not name, reconstruct, summarize, embellish, or divide the unseen action sequence into invented moves, shots, or timestamps.\n- Use weak_reference only when the user explicitly asks for loose inspiration, broad rhythm, pacing, or style rather than the same action.\n- A request to follow a video's performance does not authorize its soundtrack. Never define <Audio N>, mention music from the video, synchronize to its audio, or add an audio task type unless the user explicitly requests audio reuse/reference.\n\nSECTION REQUIREMENTS:\n- subject_definitions: one natural English line per separately tracked reference item, stating its source, role, and known defining characteristics.\n- summary: one short English paragraph beginning with the exact square-bracketed task-type prefix.\n- retention_analysis: one line per defined reference label, with shot/frame scope where applicable, the correct fixed marker, and a concrete explanation.\n- detailed_description: establish style in one or two sentences before [Shot 1], then describe the target in playback order. For each shot include known composition, subject appearance and position, environment and lighting, actions/state changes, camera, current sound, and where references take effect. Generation tasks should normally be detailed, except that unseen motion copied from a referenced video must remain an explicit exact-transfer relationship rather than invented action sequence.\n- [Shot 1] has no timestamp. Later shots use strictly increasing \"[Shot N] At MM:SS.mmm, ...\" cut times within the requested duration.\n- Use stable speaker IDs (S1), (S2). Put only actual user-provided dialogue or lyrics inside <d>[Language] ...</d>; do not invent speech.\n- overall_soundscape is 1-4 English sentences about ambience, physical sounds, and non-verbal human sounds. Do not repeat dialogue. Use N/A only for explicitly requested complete silence.\n- non_diegetic_music is 1-3 English sentences describing audience-only instrumentation, tempo, rhythm, and dynamics. Use N/A when none is requested or described. Diegetic music belongs in detailed_description.\n- When reference audio is used, state its copy/reference relationship only in the matching audible layer. Never write vague values such as \"from Video 1\" without a defined Audio label and relationship.\n\nFINAL REF2VA VALIDATION BEFORE OUTPUT:\n- If exact video motion was requested, the motion Subject is fully_preserved, and detailed_description explicitly tells the target Subject to follow that exact motion from <Video N> without listing invented moves.\n- If audio was not explicitly requested, there is no <Audio N>, no audio task type, no soundtrack copied from the video, and non_diegetic_music is N/A unless the user separately requested music.\n- Never infer action sequence details, a setting, camera cuts, timestamps, sound, or music from a referenced video that was not actually provided to the prompt-writing model.\n\nWrite the six sections in English while preserving user-provided dialogue, lyrics, and visible text in their original language. Match the exact requested duration, use concrete known details, avoid plot-summary shorthand, and output no unresolved labels or placeholders.";
const ORIGINAL_REF2VA_ENFORCEMENT = "Ref2VA enforcement: If the request says a pictured subject must copy, follow, or perform the actions or movements from a referenced video, preserve the exact actions, expressions, movements, and timing from the referenced video. Express this as a reference relationship to the motion Subject sourced from <Video N>; do not invent or paraphrase individual movements. Do not create or use <Audio N> unless the request explicitly asks to copy or reference audio.";

export function recoverRef2VARequest(assembled, body) {
  if (body.mode !== 'Reference') return assembled;
  const images = assembled.media_inputs.filter(item => item.type === 'image');
  let text = `Mode: Ref2VA\nDuration: ${body.duration_seconds} seconds\n`;
  if (body.visual_style) text += `Style: ${body.visual_style}\n`;
  text += `\nDescription:\n${body.description ?? body.creative_brief ?? assembled.input.creative_brief}`;
  if (body.reference_description) text += `\n\nReference Assets:\n${body.reference_description}`;
  if (images.length) text += `\n\nUploaded reference images available to the model: ${images.map(item => item.reference).join(', ')}.`;
  text += '\n\n' + ORIGINAL_REF2VA_ENFORCEMENT;
  // The old tool had no refinement button. Keep explicit user revisions, using
  // the recovered prompt and the same direct generation path without repair.
  if (body.instruction) text += `\n\nCurrent prompt:\n${body.current_prompt}\n\nRevision instruction (takes precedence):\n${body.instruction}`;
  const custom = typeof body.system_prompt_override === 'string' && body.system_prompt_override.trim();
  const system = custom || REF2VA_SYSTEM_PROMPT;
  return {
    ...assembled,
    input: { ...assembled.input, workflow: 'original_ref2va' },
    media_inputs: images,
    supporting_guides: [],
    system_prompt: { custom: !!custom, content: system },
    messages: [{ role: 'system', name: 'ref2va_system', content: system }, { role: 'user', content: text }],
  };
}
